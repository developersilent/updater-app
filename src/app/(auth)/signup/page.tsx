import { SignUpForm } from "@/components/auth-ui/Singup";

export default function SignUpPage() {
  return (
    <div className="w-full min-h-screen flex items-center justify-center">
      <SignUpForm />
    </div>
  );
}
